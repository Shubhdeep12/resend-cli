---
"@shubhdeep12/resend-cli": minor
---

- Switched to `resend auth ...` commands with improved key management (`login/logout/whoami/list/select`) and removed legacy `init`.
- Refactored internals into constants/formatters/validators/types plus config/auth services for cleaner structure.
